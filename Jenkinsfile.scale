pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Point kubectl to Minikube') {
            steps {
                bat 'kubectl config use-context minikube'
            }
        }

        stage('Scale Pod Resources') {
            steps {
                bat 'bash scripts/scale-resource.sh'
            }
        }
    }
}
